import { TouchKeyboard } from './touch-keyboard';

describe('TouchKeyboard', () => {
  it('should create an instance', () => {
    const directive = new TouchKeyboard();
    expect(directive).toBeTruthy();
  });
});
